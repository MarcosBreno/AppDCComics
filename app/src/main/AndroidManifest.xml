<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.etecia.androidnaruto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".cacadorActivity"></activity>
        <activity android:name=".batmanActivity" />
        <activity android:name=".atomoActivity" />
        <activity android:name=".asanoturnaActivity" />
        <activity android:name=".arqueiroActivity" />
        <activity android:name=".aquamanActivity" />
        <activity android:name=".DCActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>